#include <stdio.h>

int main()
{
    // Write a program to print average of three number
    int a, b, c, avg;
    a = 1;
    b = 2;
    c = 3;
    avg = (a + b + c) / 3;
    printf("The average of the three number is : %d ", avg);
    return 0;
}